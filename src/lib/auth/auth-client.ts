/**
 * @fileoverview Browser client for the portal authentication service.
 * @module lib/auth/auth-client
 */

import { createAuthClient } from "better-auth/react";

export const authClient = createAuthClient();
